version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile.dev
    container_name: cortexgrid-api-dev
    command: sh -c "cd apps/api && pnpm run dev"
    volumes:
      - ./apps/api/src:/app/apps/api/src:ro
      - ./apps/api/prisma:/app/apps/api/prisma:ro
      - ./apps/api/tsconfig.json:/app/apps/api/tsconfig.json:ro
      - ./apps/api/tsconfig.build.json:/app/apps/api/tsconfig.build.json:ro
      - ./apps/api/nest-cli.json:/app/apps/api/nest-cli.json:ro
    ports:
      - "3001:3001"
      - "9229:9229"
    environment:
      - NODE_ENV=development
      - DATABASE_URL=postgresql://cortexgrid:cortexgrid@postgres:5432/cortexgrid?schema=public
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - MQTT_BROKER_URL=mqtt://mosquitto:1883
    env_file:
      - .env

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile.dev
    container_name: cortexgrid-web-dev
    command: sh -c "cd apps/web && pnpm run dev"
    volumes:
      - ./apps/web/src:/app/apps/web/src:ro
      - ./apps/web/public:/app/apps/web/public:ro
      - ./apps/web/next.config.js:/app/apps/web/next.config.js:ro
      - ./apps/web/tailwind.config.ts:/app/apps/web/tailwind.config.ts:ro
      - ./apps/web/postcss.config.js:/app/apps/web/postcss.config.js:ro
      - ./apps/web/tsconfig.json:/app/apps/web/tsconfig.json:ro
    ports:
      - "3000:3000"
      - "9230:9229"
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_API_URL=http://api:3001
      - NEXT_PUBLIC_WS_URL=ws://api:3001
    env_file:
      - .env

  iot-simulator:
    build:
      context: .
      dockerfile: apps/iot-simulator/Dockerfile.dev
    container_name: cortexgrid-iot-simulator-dev
    command: sh -c "cd apps/iot-simulator && pnpm run dev"
    volumes:
      - ./apps/iot-simulator/src:/app/apps/iot-simulator/src:ro
      - ./apps/iot-simulator/tsconfig.json:/app/apps/iot-simulator/tsconfig.json:ro
    environment:
      - NODE_ENV=development
      - MQTT_BROKER_URL=mqtt://mosquitto:1883

  prometheus:
    volumes:
      - ./docker/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
